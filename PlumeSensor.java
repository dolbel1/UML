package airquality.sensors;

/**
 * @author brian_b, adapted by claire_c
 *
 */
public class PlumeSensor extends GenericSensor {

	// variables not shown (e.g., format for timestamps etc.)

	public PlumeSensor(String sID, String sensorType) {
		super(sensorType);
		this.setSensorID(sID);
	}
	
	// other methods and variables not shown

	
}
